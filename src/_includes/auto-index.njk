---
layout: article
title: Auto-index
tags: index
eleventyComputed:
  header: "{{title}}"
---
{# when calling auto-index #}
  <section id="mainTitle">
    <h1>Index of "{{indexTag}}"</h1>
  </section>
  
  <article>
    {%- for post in collections[indexTag] -%}
      <li><a href="{{post.url}}">{{ post.data.title }}</a></li>
    {%- endfor -%}
  </article>
  
  {{content | safe}}